title: 'Theme Objects'
type: object
properties:
  Global:
    description: Data available to most stencil theme templates.
    type: object
    allOf:
      - $ref: ./_global.yml
  'templates/pages/product.html':
    description: Data available to the default product page template and it's components.
    type: object
    allOf:
      - $ref: ./product.yml
  'templates/pages/compare.html':
    description: roperty to display an array of products on product comparison pages.
    type: object
    allOf:
      - $ref: ./model.yml
  Template:
    description: Description.
    type: object
    allOf:
      - $ref: ./model.yml